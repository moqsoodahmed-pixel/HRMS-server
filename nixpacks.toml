[phases.setup]
aptPkgs = ["python3", "python3-pip", "python3-venv"]

[phases.install]
cmds = ["npm ci --omit=dev", "pip3 install reportlab pillow --break-system-packages || pip3 install reportlab pillow"]